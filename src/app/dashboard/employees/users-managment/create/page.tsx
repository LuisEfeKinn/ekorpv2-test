import { UserManagementCreateView } from 'src/sections/users-managment/view';

// ----------------------------------------------------------------------

export const metadata = { title: `Create Employee` };

export default function Page() {
  return <UserManagementCreateView />;
}