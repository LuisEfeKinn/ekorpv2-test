import { SkillsCreateView } from 'src/sections/skills/view';

// ----------------------------------------------------------------------

export const metadata = { title: `Create Skills` };

export default function Page() {
  return <SkillsCreateView />;
}