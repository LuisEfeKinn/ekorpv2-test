import { PositionCreateView } from 'src/sections/positions/view';

// ----------------------------------------------------------------------

export const metadata = { title: `Create Position` };

export default function Page() {
  return <PositionCreateView />;
}